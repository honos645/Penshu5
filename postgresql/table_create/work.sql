--
-- work table
--
CREATE TABLE work(
    id varchar(8),  --学籍番号
    industry_id varchar(30),  -- 就職先コード
    Employment_name varchar(30),  --就職先名
    Employment_subject varchar(30),  --就職先所在地件名
    PRIMARY KEY (id)
    );
